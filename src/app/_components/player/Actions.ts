
export type action = {
    i: number,
    j: number,
    value: number
}
// This ones over here are the probailities of the actions
export type PossibleAction = {
    i: number,
    j: number,
    value: number[]
}